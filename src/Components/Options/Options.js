import React from "react";
import "./Options.css";

const Options = ({ option }) => {
  return (
    <div className="col-lg-6 col-sm-12 option hover ">
      <p>{option}</p>
    </div>
  );
};

export default Options;
// kaj kortisilam
